{
  "name": "@xcube/pipeline",
  "version": "1.0.0",
  "description": "Developer onboarding CLI — pulls initiative context and launches Claude Code for implementation",
  "bin": {
    "pipeline": "./dist/cli.js"
  },
  "files": [
    "dist",
    ".env.example",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "dev": "npx ts-node src/cli.ts"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^12.1.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
