{
  "title": "Figma Mockups: In-App Messaging & Trade Chat — xCube",
  "figma_file_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging",
  "figma_write_status": "created",

  "screens_created": [
    {
      "name": "Chat Tab — Channel List",
      "frame_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging?node-id=501-1",
      "prd_journeys": ["Discover and join a channel", "See unread message activity"],
      "description": "The primary chat entry point showing all public channels, ordered by recent activity. Unread counts and last message preview help users prioritise.",
      "layout_notes": "Existing BottomNavBar with Chat tab active. Channel rows reuse the ListItem/Channel component (unread variant for unseen channels). Pull-to-refresh supported.",
      "interactions": [
        { "trigger": "tap channel row", "target_screen": "Channel Detail", "notes": "Passes channel id and name" },
        { "trigger": "tap + Create", "target_screen": "Create Channel", "notes": "FAB in top right" }
      ]
    },
    {
      "name": "Channel Detail — Message Thread",
      "frame_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging?node-id=501-2",
      "prd_journeys": ["Send a message in a channel", "Attach a ticker card to a message"],
      "description": "Real-time message thread for a single channel. Messages appear chronologically with sender avatar and timestamp. Ticker cards are inline with expandable price widget.",
      "layout_notes": "Fixed header with channel name, member count, back chevron. Compose bar pinned above safe area with a chart-icon button to attach a ticker.",
      "interactions": [
        { "trigger": "tap back", "target_screen": "Chat Tab — Channel List", "notes": "" },
        { "trigger": "tap send", "target_screen": "Channel Detail — Message Thread", "notes": "Optimistically appends sent bubble" },
        { "trigger": "tap ticker card", "target_screen": "Ticker Card Detail", "notes": "Expands to full quote view" },
        { "trigger": "tap chart icon in compose", "target_screen": "Ticker Search", "notes": "Opens ticker search to attach" }
      ]
    },
    {
      "name": "Ticker Search — Attach to Message",
      "frame_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging?node-id=501-3",
      "prd_journeys": ["Attach a ticker card to a message"],
      "description": "Bottom sheet triggered from the compose bar. User searches by ticker symbol or company name. Tapping a result attaches a TickerCard to the pending message.",
      "layout_notes": "Reuses the existing Sheet pattern and InstrumentSearchResult list item, showing last price, change %, and market status inline.",
      "interactions": [
        { "trigger": "tap result", "target_screen": "Channel Detail — Message Thread", "notes": "Returns to thread with ticker card pre-attached in compose" },
        { "trigger": "tap dismiss / swipe down", "target_screen": "Channel Detail — Message Thread", "notes": "" }
      ]
    },
    {
      "name": "Ticker Card Detail",
      "frame_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging?node-id=501-4",
      "prd_journeys": ["View live ticker data from a message"],
      "description": "Expanded view of a TickerCard tapped from a message. Shows live price, intraday chart, bid/ask, volume, and a Buy / Sell CTA linking to the existing order flow.",
      "layout_notes": "Reuses the existing TickerCard/expanded and QuoteChart components as the header. Buy/Sell CTAs hand off to the existing order flow — out of scope here.",
      "interactions": [
        { "trigger": "tap back", "target_screen": "Channel Detail — Message Thread", "notes": "" },
        { "trigger": "tap Buy / Sell", "target_screen": "Order Flow (existing)", "notes": "Hands off to existing order flow — out of scope for messaging" }
      ]
    },
    {
      "name": "Create Channel",
      "frame_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging?node-id=501-5",
      "prd_journeys": ["Create a new public channel"],
      "description": "Simple form to create a new public channel. Name (required, unique), optional topic, and a Create button. Inline validation on name uniqueness.",
      "layout_notes": "Two stacked inputs — Name (50 char limit) and Topic (optional, 140 char limit) — with the Create button pinned above the keyboard safe area.",
      "interactions": [
        { "trigger": "tap Create", "target_screen": "Channel Detail — Message Thread", "notes": "Navigates to newly created channel on success" },
        { "trigger": "tap Cancel / back", "target_screen": "Chat Tab — Channel List", "notes": "" }
      ]
    },
    {
      "name": "Notification Settings — Channel Mute Controls",
      "frame_url": "https://www.figma.com/file/aBcD1234xCubeMsgMock/In-App-Messaging?node-id=501-6",
      "prd_journeys": ["Discover and join a channel"],
      "description": "A basic per-channel mute toggle so a member can stop push notifications for one channel without leaving it.",
      "layout_notes": "Single switch row in the channel detail overflow menu — reuses the existing Switch/Toggle component and ListItem row pattern, no new screen chrome needed.",
      "interactions": [
        { "trigger": "tap overflow in Channel Detail header", "target_screen": "Notification Settings — Channel Mute Controls", "notes": "Opens as a small menu with the mute toggle as the first row" },
        { "trigger": "toggle mute switch", "target_screen": "Notification Settings — Channel Mute Controls", "notes": "Persists immediately, no confirmation step" }
      ]
    }
  ],

  "navigation_flow": "BottomNav (Chat Tab) → Channel List\nChannel List → Channel Detail (tap row)\nChannel List → Create Channel (tap +)\nChannel Detail → Ticker Search (tap chart icon)\nChannel Detail → Ticker Card Detail (tap ticker card)\nChannel Detail → Notification Settings (tap overflow)\nTicker Search → Channel Detail (select ticker)\nCreate Channel → Channel Detail (on success)",

  "design_gaps": [
    "ModeratedMessagePlaceholder — a message bubble variant showing a 'Message under review' state with a pending badge; not in the design system",
    "ChannelEmptyState — full-screen empty state illustration for channels with zero messages; needs a new illustration asset"
  ],

  "notes": "6 frames written to Figma file aBcD1234xCubeMsgMock. Design gaps flagged for the designer to add during review. Awaiting designer review: open the Figma file, refine spacing and copy, then mark complete in xCube Flow to advance the workflow."
}
